apiVersion: v1
kind: Service
metadata:
  name: {{ include "join-service.fullname" . }}
  labels:
    {{- include "join-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    {{- if .Values.metricsPort }}
    - port: {{ .Values.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- end }}
  selector:
    {{- include "join-service.selectorLabels" . | nindent 4 }}
