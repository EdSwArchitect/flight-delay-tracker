apiVersion: v1
kind: Service
metadata:
  name: {{ include "map-ui.fullname" . }}
  labels:
    {{- include "map-ui.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "map-ui.selectorLabels" . | nindent 4 }}
